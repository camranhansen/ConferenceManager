package csc.zerofoureightnine.conferencemanager.menu;

/**
 * The goal flag. Denotes where the current csc.zerofoureightnine.conferencemanager.menu layer wants to go from here.
 * The names are self-explanatory. Can be called anywhere in the program.
 * Parsed in Menu.
 */
public enum MenuGoal {
    CONTINUE,
    BACK,
    MAIN,
    LOGOUT,
};
