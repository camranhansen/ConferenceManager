package csc.zerofoureightnine.conferencemanager.menu;

/**
 * Responsible for user and permission-related functionality. Permissions were merged with the user methods since they are essentially linked,
 * In the fact that all permission methods are affecting users.
 */
public class UserMenuLayer extends MenuLayer {
    //Also has permission management
}
